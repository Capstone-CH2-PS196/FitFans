<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Fitfans" parent="Theme.Material3.Light">
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/primary</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="actionBarStyle">@style/ActionBarStyle</item>
        <item name="actionBarTheme">@style/ActionBarTheme</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <style name="ActionBarStyle" parent="Widget.Material3.ActionBar.Solid">
        <item name="titleTextStyle">@style/ActionBarTitle</item>
        <item name="background">@color/primary</item>
    </style>

    <style name="ActionBarTheme" parent="ThemeOverlay.Material3.Toolbar.Surface">
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="ActionBarTitle" parent="TextAppearance.Material3.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Splash Screen Settings -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/fitfans</item>
        <item name="postSplashScreenTheme">@style/Theme.Fitfans</item>
    </style>
</resources>